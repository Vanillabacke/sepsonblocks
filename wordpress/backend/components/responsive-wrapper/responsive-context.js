

const { createContext } = wp.element;

export const ResponsiveContext = createContext({
	wrapperState:   null,
	// values:         undefined,
	// value:          undefined,
	// attributeName:  undefined,
});